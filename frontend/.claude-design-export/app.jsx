/* Phoskonomia — final composed dashboard (Console hero + Terminal detail). */
function PhoskApp() {
  return <window.DashFull />;
}
ReactDOM.createRoot(document.getElementById("root")).render(<PhoskApp />);

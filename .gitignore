# build artifacts
target/
**/node_modules/
frontend/app/dist/
frontend/dioxus-app/dist/
.dioxus/

# runtime data — encrypted photos, keys, local databases. NEVER commit.
phosk-data/
*.db
*.surrealkv

# secrets
.env
.env.*
!.env.example
*.pem
*.key
*.p12
id_rsa*
id_ed25519*
.netrc

# local-only assistant context — may describe this machine; the repo is public
CLAUDE.md
CLAUDE.local.md
.claude/

# local agent automation (scripts, state, logs, worktrees) — machine-specific
agentic-loop/

# editor / OS
.idea/
.vscode/
*.swp
.DS_Store
